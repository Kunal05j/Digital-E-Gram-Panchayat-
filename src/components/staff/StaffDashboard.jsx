import React from 'react';


export default function StaffDashboard({ appUser }) {
    return (
        <div>
            <h2>Staff Dashboard</h2>
            <p>Select a service to view applications in the admin panel (or use APIs to fetch).</p>
        </div>
    );
}